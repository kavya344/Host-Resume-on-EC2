name: resume-upload-action

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
permissions:
  packages: write

jobs:
    deploy:
      runs-on: ubuntu-latest
      name: resume-push
      steps:
      - name: checkout repo
        uses: actions/checkout@v4
